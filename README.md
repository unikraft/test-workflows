# lib-libc-test actions

In each pull request, use [lib-lic-test](https://github.com/unikraft/lib-libc-test) 
to check if the PR would break existing libc compatibility, and if so, it will prevent the merge. 
